# Framer Server API credentials — used by `pnpm framer:sync`.
# Create an API key in Framer: open a project → Cmd+K → "Open Settings" → API Keys.
FRAMER_API_KEY=

# Project URL (or project id) to sync code files into.
FRAMER_PROJECT_URL=https://framer.com/projects/Your-Project--aabbccdd1122
